#include "src/PlayerInfo/Player.h"
#include <iostream>
#include <string>

int main()
{
    std::string name;
    int age, height;
    std::cout << "Enter the name for your character: " << '\n';
    std::cout << "> ";
    std::getline(std::cin, name);
    std::cin.ignore();

    std::cout << "Enter the age of your character" << '\n';
    std::cout << "> ";
    std::cin >> age;

    Player

    return 0;
}